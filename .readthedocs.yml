version: 2

sphinx:
  builder: html
  configuration: doc/conf.py

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"
  apt_packages:
    - doxygen

python:
  install:
    - requirements: doc/requirements-rtd.txt
